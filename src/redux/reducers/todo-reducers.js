const initialState = {
    todo:[],
    isLoading: false
}


function todoReducer(state=initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default todoReducer